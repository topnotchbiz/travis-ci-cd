python:
  - 3.6
  - 3.7

jobs:
  include:
    - os: linux
      dist: xenial
      language: python
    - os: linux
      dist: bionic
      language: python
    - os: osx
      osx_image: xcode9.3
      language: minimal

install:
  - pip install -r requirements.txt
# command to run tests
script:
  # unit test
  - python -m pytest -v