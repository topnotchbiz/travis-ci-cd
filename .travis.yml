language: python
jobs:
  include:
    - os: linux
      dist: xenial
      python: 3.6
    - os: linux
      dist: xenial
      python: 3.7
    - os: linux
      dist: bionic
      python: 3.6
    - os: linux
      dist: bionic
      python: 3.7
    - os: osx
      osx_image: xcode10
      language: generic
      env: PYTHON=36

install:
  - pip install -r requirements.txt
# command to run tests
script:
  # unit test
  - python -m pytest -v